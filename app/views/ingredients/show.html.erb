<h1><%= @ingredient.name %></h1>

<p>Category: <%= @ingredient.category.name %></p>
<p>Location: <%= @ingredient.location.name %></p>
<p>Expires: <%= @ingredient.expire.strftime("%d-%m-%Y") %></p>


<%= link_to "[Edit]", edit_ingredient_path( @ingredient.id ) %>
<%= link_to "[Delete]", ingredient_path( @ingredient.id ), method: :delete, data: { confirm: "Are you sure?" } %>
<%= link_to "VIEW RECIPES", "http://allrecipes.com/search/results/?wt=#{@ingredient.name}&sort=re" %>
