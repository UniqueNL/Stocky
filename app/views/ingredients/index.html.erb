<h1>Ingredients</h1>

<!-- Listing all items inside the ingredients. -->
<% @ingredients.each do |ingredient| %>
  <p><%= link_to ingredient.name, ingredient_path(ingredient.id) %><%=  ingredient.category.name  %><%=  ingredient.amount  %></p>
<% end %>
