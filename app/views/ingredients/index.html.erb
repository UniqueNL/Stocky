<h1>Ingredients</h1>

<% @ingredients.each do |ingredient| %>
  <p><%= link_to ingredient.name, ingredient_path(ingredient.id) %><%= ingredient.category.name %></p>
<% end %>

<%= link_to "[New ingredient]", new_ingredient_path %>
