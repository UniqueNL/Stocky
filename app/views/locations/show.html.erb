<h1><%= @location.name %></h1>


<%= link_to "[Edit]", edit_location_path( @location.id ) %>
<%= link_to "[Delete]", location_path( @location.id ), method: :delete, data: { confirm: "Are you sure?" } %>
