<h1>Categories</h1>

<!-- Listing all items inside the ingredients. -->
<% @categories.each do |category| %>
  <p><%= link_to category.name, category_path(category.id) %></p>
<% end %>
<br>
<%= link_to "+ Add a category", new_ingredient_path %>
