<%= form_for @category do |i| %>
  <%= i.label :name %>
  <%= i.text_field :name %>

  <!--if new record, button text is "create" , else it is "update"-->
  <%= i.submit (@category.new_record? ? "Create" : "Update") %>
<% end %>
